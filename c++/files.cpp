#include <iostream>
#include <fstream>

using namespace std;

int main()
{
    /*
        END OF FILE (text = 1A or 26) (binary = ctrl+z)
        END OF LINE (text = CR/LF)
    */

//    ifstream f1;
//    f1.open('a.dat', ios::in);
//    f1.close();
//    ifstream f2("a.dat", ios::in);

    // ofstream f2;
    // f2.open("a.dat", ios::out);

    // fstream f3("a.dat", ios::in | ios::out) // ios::app, ios::ate, ios::binary, ios::trunc, 

    // eof(); // is end of file?


    // char ch;
    // ofstream f1("a.dat"); // ios::binary
    // if(!f1)
    //     exit(0);
    // cout<<"Enter message:\n";
    // do{
    //     cin.get(ch);
    //     f1.put(ch);
    // } while(ch != '$');
    // f1.close();

    // cout<<"output:\n";
    // ifstream f2("a.dat");
    // f2.get(ch);
    // while(!f2.eof())
    // {
    //     cout.put(ch);
    //     f2.get(ch);
    // }

    // f2.close();


}